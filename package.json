{
  "name": "wc3-ts-template",
  "version": "1.0.0",
  "description": "",
  "author": "TriggerHappy",
  "license": "MIT",
  "main": "src-ts/main.ts",
  "scripts": {
    "test": "tstl -p tsconfig.json --rootDir \"src-ts\" && node bin/operations run target/map.w3x",
    "build": "tstl -p tsconfig.json --rootDir \"src-ts\" && node bin/operations build map.w3x",
    "build-test": "tstl -p tsconfig.json --rootDir \"src-ts\" && node bin/operations build map.w3x && node bin/operations run target/map.w3x",
    "dev": "npm-watch",
    "build:defs": "node bin/operations gen-defs"
  },
  "watch": {
    "build:defs": {
      "patterns": [
        "./maps/map.w3x"
      ],
      "extensions": "lua",
      "legacyWatch": true
    }
  },
  "dependencies": {
    "npm-watch": "^0.6.0",
    "typescript-to-lua": "^0.27.1",
    "war3tstlhelper": "^1.0.0"
  },
  "devDependencies": {
    "lua-types": "^2.6.1"
  }
}
